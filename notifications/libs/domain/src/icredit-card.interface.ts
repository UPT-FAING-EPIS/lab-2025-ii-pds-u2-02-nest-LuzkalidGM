// This file was previously holding the IMessageSender definition by mistake.
// Rename to ICreditCard (placeholder) to avoid duplicate interface names.
export interface ICreditCard {
  // define credit-card-related fields here when needed
}